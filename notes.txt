edit->stage->commit->track        git lifecycle


//git basics commands

git init
git status               -s flag for summary      
git add                   *.py  for same time of files
git commit -m ""
git commit -am ""            --stage and commit at one time only if git already tracks 
git restore --staged ""           -unstage file
///////////////////


//configuration

git config user.name ""          -sets local 
git config user.email ""

git config --global user.name ""      -sets for all repos 

git config --list             -lists all configuration
//////////////////////

git restore "file_name"     -restore file changes

git log            --oneline / --oneline -5 / --oneline --graph --all its a graph view

***
//.env       is an environment variable file with key=value pairs for sensitive data user=proto 
//.gitignore is a file where we can write file name to ignore for tracking and
             folderName/ for a folder to ignore folderName/file_name for specific file
***

//git doesn't track empty folder for tracking put .gitkeep file in it